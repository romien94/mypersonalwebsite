.hero {
    height: 100vh;
    color: white;
}

.hero__header {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    @include tablets {
        margin-bottom: 18vh;
    }
    @include phones {
        margin-bottom: 19vh;
    }
}

.hero__container {
    display: flex;
    align-items: center;
    flex-direction: column;
    height: 100%;
    justify-content: space-between;
    align-items: center;
    padding: 2vh 0 5vh 0;
        @include desktop {
            padding: 2vh 1% 5vh 2.5%
        }
        @include tablets {
            justify-content: flex-start;
            padding-top: 6vh;
            position: relative;
        }
        @include phones {
            padding-top: 9vh;
        }
}

.hero__content {
    display: flex;
    position: relative;
    @include tablets {
        flex-direction: column;
    }
}

.hero__sitename {
    width: 100%;
    position: absolute;
    display: flex;
    flex-direction: column;
    opacity: 0.3;
    text-align: right;
    top: 0;
    right: 120%;
    @include tablets {
        font-size: 16px;
        position: static;
        text-align: center;
        font-weight: 600;
        margin-bottom: 3vh;
    }
    @include tablets {
        font-size: 14px;
        margin-bottom: .5vh;
    }
}

.hero__title {
    display: flex;
    flex-direction: column;
    font-size: 72px;
    font-weight: 800;
    line-height: 0.83;
    text-align: center;
    @include tablets {
        font-size: 48px;
    }
    @include phones {
        font-size: 36px;
        line-height: 1.33;
    }
}

.hero__buttons {
    @include tablets {
        display: none;
    }
}

.hero__scroll-button {
    display: flex;
    align-items: center;
    font-size: 18px;
    font-weight: 600;
}

.hero__scroll-button:hover {
    color: #5500f2;
}

.hero__scroll-button:hover .hero__scroll-img {
    fill: #5500f2;
}

.hero__scroll-title {
    margin-right: 10px;
}

.hero__scroll-img {
    width: 30px;
    height: 30px;
    fill: white;
}

.hero__links {
    @include tablets {
        flex: 1;
    }
    @include phones {
        display: none;
    }
}